import 'package:flutter/material.dart';

import 'screens/connection_screen.dart';

class RoverEntryScreen extends StatelessWidget {
  const RoverEntryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const RoverConnectionScreen();
  }
}
