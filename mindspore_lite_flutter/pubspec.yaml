name: mindspore_lite_flutter
description: Flutter plugin for MindSpore Lite AI inference
version: 0.0.1
homepage:

environment:
  sdk: ">=2.19.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.example.mindspore_lite_flutter
        pluginClass: MindsporeLiteFlutterPlugin
      ios:
        pluginClass: MindsporeLiteFlutterPlugin
